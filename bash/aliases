# Easy navigation & listing
alias ..="cd .."
alias lsa="ls -AF"
alias lso="ls -AF1"
alias lsl="ls -AFhl"
alias unixtime="date +%s"

# Create dir and move into it
function take() {
	mkdir $1
	cd $1
}

# Shortcut to directories
alias ht="cd /Applications/XAMPP/htdocs"
alias hm="cd ~"

# Start/stop XAMPP
alias xampp="/Applications/XAMPP/localhost"
alias xampp-stop="/Applications/XAMPP/localhost-stop"

# Launch MySQL terminal
alias mysql="/usr/local/mysql/bin/mysql -u root"

# Start/stop MySQL process
alias mysql-start="sudo /library/startupitems/mysqlcom/mysqlcom start"
alias mysql-stop="sudo /library/startupitems/mysqlcom/mysqlcom stop"

# Toggle hidden files in Finder
alias showhidden="defaults write com.apple.finder AppleShowAllFiles TRUE; killall Finder"
alias hidehidden="defaults write com.apple.finder AppleShowAllFiles FALSE; killall Finder"

# Restart wireless
alias resetip="sudo ipconfig set en1 BOOTP; sudo ipconfig set en1 DHCP"

# Re-load aliases
alias reload="source ~/.bin/dotfiles/bash/aliases"
alias ea="mvim ~/.bin/dotfiles/bash/aliases && reload"
